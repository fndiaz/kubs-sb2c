apiVersion: v1
kind: Service
metadata:
  name: service-api-front
  labels:
    name: service-api-front

spec:
  type: LoadBalancer
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30002
  selector:
    app: api-front