apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-mongo-gb
  labels:
    name: deploy-mongo-gb
    type: mongo

spec:
  template:
    metadata:
      name: mongo-gb
      labels:
        app: mongo-gb
        type: mongo
    spec:
      containers:
        - name: mongo-gb
          image: mongo:3.4.19
          ports:
            - containerPort: 27017
  replicas: 1
  selector:
    matchLabels:
      app: mongo-gb