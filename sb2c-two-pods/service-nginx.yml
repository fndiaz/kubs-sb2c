apiVersion: v1
kind: Service
metadata:
  name: service-nginx-gb
  labels:
    name: service-nginx-gb

spec:
  type: LoadBalancer
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30002
  selector:
    app: nginx-gb